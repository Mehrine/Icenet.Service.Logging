using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.InteropServices.ComTypes;
using System.Text;
using System.Threading.Tasks;
using Icenet.Service.Logging.UI.Helpers;
using Icenet.Service.Logging.UI.Models;
using NUnit.Framework;
using NUnit.Framework.Internal;

namespace Icenet.Service.Logging.UI.UnitTests.Helper
{
    //    [TestFixture]
    //    class ReadFileUsingStreamReaderTest
    //    {

    //    //    [Test]
    //    //    public void bbbbbb()
    //    //    {
    //    //        var TestRetreiverOfLogItems = new RetrieverOfLogItems();
    //    //        var result = TestRetreiverOfLogItems.GetLogItemInfoFromJsonFile();

    //    //        Assert.That(result, Is.Not.Null);
    //    //        Assert.That(result.Count, Is.EqualTo(5));
    //    //        //var logItems = result.Where(u => u.Title=="Testing");
    //    //        //var logDetail = result.Where(d => d.Detail == "Quick check to ensure data prints to the table");
    //    //        //var logLevel = result.Where(l => l.LogLevel == "Info");

    //    //        Assert.That(logItems.Count(),Is.EqualTo(1));
    //    //        Assert.That(logDetail.Count(), Is.EqualTo(1));
    //    //        Assert.That(logLevel.Count(), Is.EqualTo(3));

//}
//    }
}
